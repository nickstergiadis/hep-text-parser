# HEP Builder Pro - Parser Quality Patch

This patch focuses on better parsing quality for rough clinician shorthand.

## Improvements
- better cleanup of leftover parser junk in notes
- better alias handling for common exercises
- improved handling of modifiers such as `with band` and `different intensities`
- instructions render in editable cards, preview, copied summary, and email draft
- still works as a static GitHub Pages app

## Replace in repo root
- index.html
- style.css
- app.js
